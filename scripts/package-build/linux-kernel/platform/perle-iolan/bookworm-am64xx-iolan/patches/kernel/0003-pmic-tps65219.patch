diff --git a/arch/arm64/boot/dts/ti/k3-am642-iolan.dts b/arch/arm64/boot/dts/ti/k3-am642-iolan.dts
index 89be4f7c714a..ae328ab89d9a 100644
--- a/arch/arm64/boot/dts/ti/k3-am642-iolan.dts
+++ b/arch/arm64/boot/dts/ti/k3-am642-iolan.dts
@@ -400,7 +400,7 @@ AM64X_IOPAD(0x02a8, PIN_OUTPUT, 0) /* (E19) USB0_DRVVBUS */
 	/* PMIC_EXTINT# */
 	pinctrl_pmic_extint: main-system-extint-default-pins {
 		pinctrl-single,pins = <
-			AM62X_IOPAD(0x0278, PIN_INPUT, 0) /* (C19) EXTINTn */
+			AM64X_IOPAD(0x0278, PIN_INPUT, 7) /* (C19) EXTINTn, GPIO1_70 */
 		>;
 	};

@@ -486,6 +486,7 @@ eeprom@50 {
 		/* AT24CM01 */
 		compatible = "atmel,24c1024";
 		reg = <0x50>;
+		vcc-supply = <&reg_3v3>;
 	};

 	rtc_rv3028: rtc@52 {
@@ -500,8 +501,10 @@ tps65219: pmic@30 {
 		reg = <0x30>;
 		pinctrl-names = "default";
 		pinctrl-0 = <&pinctrl_pmic_extint>;
-		interrupt-parent = <&gic500>;
-		interrupts = <GIC_SPI 224 IRQ_TYPE_LEVEL_HIGH>;
+		interrupt-parent = <&main_gpio1>;
+		interrupts = <70 IRQ_TYPE_LEVEL_LOW>;
+		/* interrupt-parent = <&gic500>; */
+		/* interrupts = <GIC_SPI 224 IRQ_TYPE_LEVEL_HIGH>; */

 		buck1-supply = <&reg_vsodimm>;
 		buck2-supply = <&reg_vsodimm>;
@@ -513,7 +516,6 @@ tps65219: pmic@30 {
 		system-power-controller;
 		ti,power-button;

-
 		regulators {
 			reg_vdd_core: buck1 {
 				regulator-name = "VDD_CORE";
@@ -655,6 +657,7 @@ &cpsw3g_mdio {
 	cpsw3g_phy0: ethernet-phy@0 {
 		bootph-all;
 		reg = <0>;
+		/* ti,max-output-impedance; */
 		ti,clk-output-sel = <DP83867_CLK_O_SEL_OFF>;
 		ti,rx-internal-delay = <DP83867_RGMIIDCTL_2_00_NS>;
 		ti,fifo-depth = <DP83867_PHYCR_FIFO_DEPTH_4_B_NIB>;
@@ -663,6 +666,7 @@ cpsw3g_phy0: ethernet-phy@0 {
 	cpsw3g_phy1: ethernet-phy@1 {
 		bootph-all;
 		reg = <1>;
+		/* ti,max-output-impedance; */
 		ti,clk-output-sel = <DP83867_CLK_O_SEL_OFF>;
 		ti,rx-internal-delay = <DP83867_RGMIIDCTL_2_00_NS>;
 		ti,fifo-depth = <DP83867_PHYCR_FIFO_DEPTH_4_B_NIB>;
@@ -700,6 +704,7 @@ &pcie0_rc {
 	phys = <&serdes0_pcie_link>;
 	phy-names = "pcie-phy";
 	num-lanes = <1>;
+	snps,enable-cdm-check;
 	pcie_aspm = "powersave";
 };
