# igOS model definition — IRG 1A  (platform: am64x)
#
# One directory = one model's complete definition (see
# vyos.system.model for the schema).  Sibling files, if present:
#   default-config   this model's config.boot.default
#   cli-remove       CLI nodes to prune for this model
#
# IRG 1/1A use the on-board GPIO dual-SIM mux (sim_mux=gpio): the FSM drives
# MODEM0_SIM_SELECT_1N_OR_2 and reads MODEM0_SIM_DETECT_* directly.

match    = IRG-1A00
platform = am64x
dtb      = ti/k3-am642-irg-1a.dtb

serial   = 1
cell     = builtin
sim_mux  = gpio
wifi     = false

ethernet    = 1
poe_pd      =
wifi_module =
